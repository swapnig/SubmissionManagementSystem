/**
 * Hibernate Entity classes for Category, CategoryAttribute (as each category can have 
 * multiple different attributes), Member, MemberAttribute (as each member can have its own 
 * multiple different attributes), enum MemberStausType, UserToMemberMapping (User's 
 * registration for a member)
 * 
 * @since 1.0
 * @author Pramod R. Khare, Swapnil Gupta
 * @version 1.0
 */
package edu.neu.ccis.sms.entity.categories;